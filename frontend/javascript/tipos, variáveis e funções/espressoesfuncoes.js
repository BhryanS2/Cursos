// expressão de função
const soma = (a, b) => a + b;

// declaração de função
function somaClassica(a, b) {
  return a + b;
}

// qual a diferença
console.log(print()); // Hello World
function print() {
  return "Hello World";
}

console.log(subtracao(1, 2)); // erro -> Cannot access "subtracao" before initialization
const subtracao = (a, b) => a - b;
